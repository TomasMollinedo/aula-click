// Cierra el modal al navegar al listado con un Link o router.push (por ejemplo, desde el Sidebar):
// sin esto, el slot conservaría el último modal abierto.
export default function SinModal() {
  return null
}
